{
  "name": "tasc-uae-lp",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "npm --prefix \"Tech Recruitment\" ci --include=dev && npm --prefix \"Tech Recruitment\" run build && vite build && node scripts/prepare-dist.mjs",
    "dev": "vite"
  },
  "dependencies": {
    "vite": "6.3.5",
    "typescript": "5.8.3"
  }
}
